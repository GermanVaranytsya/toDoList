function getTaskElement(name) {    return '<li class="task">' +    '<div class="text">' +    name +    '</div>' +    '<div class="icons">' +    '<div class="ml-25 state-icon">' +    '<i class="fas fa-check fa-lg"></i>' +    '<i class="fas fa-history fa-lg"></i>' +    '</div>' +    '<div class="ml-25  input-buttons remove-task">' +    '<i class="fas fa-times fa-lg"></i>' +    '</div>' +    '</div>' +    '</li>'}$(function () {    $('.fa-check').click(function (event) {        $(event.target).closest('.task').toggleClass('done');    });    $('.fa-history').click(function (event) {        $(event.target).closest('.task').toggleClass('done');    });    $('.remove-task').click(function (event) {        $(event.target).closest('.task').remove();    });    $('#hide-done').click(function () {        $('#tasks').toggleClass('done-hidden');    });    $('.fa-plus').click(function() {        let taskName = $('#add-item-input').val();        if (taskName.trim().length > 0 && taskName.trim().length < 25) {            $('#tasks').append(getTaskElement(taskName));        } else {            alert('error')        }    })});
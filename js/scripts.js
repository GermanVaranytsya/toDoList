var tasksParentSelector = '#tasks';function getTaskElement(name) {    return '' +        '<li class="task">' +        '<div class="text">' +        name +        '</div>' +        '<div class="icons">' +        '<div class="ml-25 state-icons">' +        '<i class="fas fa-check fa-lg"></i>' +        '<i class="fas fa-history fa-lg"></i>' +        '</div>' +        '<div class="ml-25  input-buttons remove-task">' +        '<i class="fas fa-times fa-lg"></i>' +        '</div>' +        '</li>'}function removeTask(event) {    $(event.target).closest('.task').fadeToggle('slow',function(){        $(this).remove();    });}function toggleTaskDoneState(event) {    $(event.target).closest('.task').toggleClass('done');}function toggleDoneTasksVisibility(event) {    $(event.target).closest('.input-buttons').toggleClass('grey');    $(tasksParentSelector).toggleClass('done-hidden');}function addTask(taskName) {    $(getTaskElement(taskName)).appendTo(tasksParentSelector).hide().fadeToggle('slow');}function checkNewTask(event) {    var taskName = $('#add-item-input').val();    if (taskName.trim().length > 0 && taskName.trim().length < 20) {        addTask(taskName);        $('#add-item-input').val('');    } else {        alert('error');    }}$(document).ready(function () {    $('#add-item-input').keypress(function(e){        if(e.which == 13){//Enter key pressed            $('#add-task').click();//Trigger search button click event        }    });    $(tasksParentSelector).on('click', '.remove-task', removeTask);    $(tasksParentSelector).on('click', '.state-icons', toggleTaskDoneState);    $('#hide-done').click(toggleDoneTasksVisibility);    $('#add-task').click(checkNewTask);});
function getTaskElement(name) {    return '' +        '<li class="task">' +        '<div class="text">' +        name +        '</div>' +        '<div class="icons">' +        '<div class="ml-25 state-icon">' +        '<i class="fas fa-check fa-lg"></i>' +        '<i class="fas fa-history fa-lg"></i>' +        '</div>' +        '<div class="ml-25  input-buttons remove-task">' +        '<i class="fas fa-times fa-lg"></i>' +        '</div>' +        '</li>'}$(document).ready(function () {    $('#tasks').on('click', '.remove-task', function (event) {        $(event.target).closest('.task').remove();    });    $('#tasks').on('click', '.state-icon', function (event) {        $(event.target).closest('.task').toggleClass('done');    });    $('#hide-done').click(function (event) {        $(event.target).closest('.input-buttons').toggleClass('grey');        $('#tasks').toggleClass('done-hidden')    });    $('#add-task').click(function (event) {        const taskName = $('#add-item-input').val();        if (taskName.trim().length > 0) {            $('#tasks').append(getTaskElement(taskName));        } else {            alert('error')        }    });});